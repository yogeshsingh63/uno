# The app automatically detects your machine's IP for mobile, and uses localhost for web.
# However, if the connection fails (especially on Android physical devices), uncomment the line below and replace with your actual Wi-Fi LAN IP address.

# EXPO_PUBLIC_SERVER_URL=http://YOUR_LAN_IP:3001
